export type EstimateDetails = {
  id: number;
  construction_id: number;
  estimate_category_id: number;
  estimate_small_category_id: number;
  detail: string;
  specification: string;
  quantity: number;
  unit: string;
  unit_price: number;
  subtotal: number;
  remarks: string;
};

export type TypeEstimateDetails = {
  id: number;
  construction_id: number;
  estimate_category_id: number;
  estimate_small_category_id: number;
  detail: string;
  specification: string;
  quantity: number;
  unit: string;
  unit_price: number;
  subtotal: number;
  remarks: string;
};
