export interface PendingApplication {
  id: number;
  applicationDate: string;
  status: string;
  classification: string;
  constructionNameCode: string;
  cooperationCompany: string;
  amount: number;
  middleItem: string;
  budgetAmount: number;
}